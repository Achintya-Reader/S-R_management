# Cloudflare Pages — no wrangler deploy needed. Pages builds static + functions/ automatically.
# Keep minimal config for local `wrangler pages dev`. Do NOT run `wrangler deploy` in Pages build.
compatibility_date = "2024-01-01"

# For Workers deployment use: wrangler pages deploy ./ --project-name=sms3
# Env vars: FIREBASE_PROJECT_ID, FIREBASE_SERVICE_ACCOUNT (optional), REQUIRE_AUTH
