rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Free tier simple: authenticated only (Firebase Auth)
      allow read, write: if request.auth != null;
    }
  }
}
